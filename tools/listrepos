#!/usr/bin/env bash
# Auto-generated by bootstrap.sh — do not edit by hand.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${CARGO_TARGET_DIR:-$SCRIPT_DIR/../target}/debug/run" "$(basename "$0")" "$@"
