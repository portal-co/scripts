# AIKEY-l4qkxonqry2b4gj7bsrkqpryiy
---
name: Rotate AI Submission Key

on: # yamllint disable-line rule:truthy
  workflow_dispatch: null
  workflow_call: null

permissions:
  contents: write

jobs:
  rotate-key:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: true
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
      - name: Install inject-key
        run: cargo install --git https://github.com/portal-co/scripts.git inject-key
      - name: Rotate AI submission key
        run: inject-key --rotate
      - name: Commit rotated key
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add key.agents_.md
          git diff --cached --quiet || git commit -m "chore: rotate AI submission key"
      - name: Push rotation commit
        run: git push
